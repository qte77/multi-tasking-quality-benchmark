### Added

- Project scaffold to estate standard: `pyproject.toml` (name=mtqb, MIT,
  Python 3.13+, pydantic 2), `Makefile` + `Makefile.python`, `.scaffold`,
  `.editorconfig`, `.gitignore`, `LICENSE` (MIT 2026), `SECURITY.md`,
  `changelog.d/`, `AGENTS.md`, `.claude/rules/`, `.claude/skills/`.
- WakaTime Pydantic models (`src/mtqb/models.py`): `SummaryProject`,
  `DailySummary`, `Project`, `AllTime` (daily_average in seconds),
  `DurationSegment`; all `strict=True, frozen=True`.
- `WakaLoader` interface (`src/mtqb/loader.py`): typed stub with
  `load_summaries`, `load_projects`, `load_all_time`, `load_durations(date)`;
  all methods raise `NotImplementedError` pending implementation.
- Loader spec tests (`tests/mtqb/test_loader.py`): 7 `xfail(strict=True)`
  TDD-red tests covering projects, all_time, summaries, durations, missing-date
  error, configurable data_dir, and envelope unwrapping; committed fixtures
  under `tests/mtqb/fixtures/`.
